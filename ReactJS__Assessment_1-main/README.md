# ReactJS M1-A1 — Professional Console Edition

Each practical task contains:
- `index.html` — simple Welcome page
- `script.js` — assessment JavaScript

The HTML pages only show **Welcome** and the task name. The external `script.js` remains attached to each page.

Open the page and use the browser Console to see the JavaScript output.
